# ── HORIZON Web — Variables de entorno ────────────────────────────────────────
# Copia este archivo como .env y completa los valores reales.
# NUNCA commitees .env al repositorio.

# ── Supabase HORIZON (proyecto principal) ─────────────────────────────────────
SUPABASE_URL=https://dkitbnrpwmwrfnmztdfc.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...   # Settings → API → service_role (PRIVADA)

# ── n8n Webhooks ───────────────────────────────────────────────────────────────
N8N_WEBHOOK_URL=https://horizon-n8n.tmae4w.easypanel.host/webhook/horizon-leads

# ── Anthropic (Agente HORIZON — Módulo D) ─────────────────────────────────────
# console.anthropic.com → API Keys
ANTHROPIC_API_KEY=sk-ant-api03-...

# ── Supabase SaaS (proyecto peluquerías — horizon-saas) ───────────────────────
# Solo service_role para endpoints de servidor. El anon key está hardcodeado en saas-config.js.
SUPABASE_SAAS_SERVICE_KEY=eyJhbGc...   # Settings → API → service_role (PRIVADA)
